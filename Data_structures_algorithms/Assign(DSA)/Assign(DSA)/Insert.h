#pragma once
#include "Algorithms.h"
class Insert :
	public Algorithms
{
public:
    void insertSorted(vector<int>& arr, int key)
    {
        //auto it = upper_bound(arr.cbegin(), arr.cend(), key);
       // arr.insert(it, key );
       
    }
};

